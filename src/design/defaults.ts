import type { UserDesignPreferences } from "./types"

export const DEFAULT_PREFERENCES: UserDesignPreferences = {
  theme: "futuristic",
  palette: "ocean-blue",
  animation: "dynamic",
  layout: "grid",
  typography: "technical",
}

export const STORAGE_KEY = "portfolio-design-preferences"
